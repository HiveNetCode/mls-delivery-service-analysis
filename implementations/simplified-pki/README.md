# Simplified PKI

This folder contains the implementation of a simplified PKI allowing MLS clients to store and retrieve Key Packages which are the principal way to add a member to the group.

This is a very simplified PKI that only allows to associate an arbitrary name, chosen by the client, to a Key Package. Then, others can use this name to retrieve the Key Package.
